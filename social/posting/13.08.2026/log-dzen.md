# Лог публикаций — dzen — 13.08.2026

## Попытка публикации — 2026-08-13 09:03 UTC (12:03 MSK)

- Пост: `post-1.md` (следующий неопубликованный, файла состояния не было —
  папка `dzen/` за 13.08.2026 содержит только `post-1.md`)
- Канал: `@HotelAI_ru`
- Способ отправки: Cloudflare Worker прокси
  `https://neuru-telegram-proxy.levshina900.workers.dev` (согласно
  инструкции v2.0, прямой вызов `api.telegram.org` не пробовали — он
  заблокирован ещё в попытках за 12.08.2026)
- Результат: **ОШИБКА** — публикация не выполнена

**Причина:** запрос к прокси заблокирован на уровне TLS/egress —
`curl` вернул `CONNECT tunnel failed, response 403`, HTTP-статус `000`
(соединение не установлено, ответа от Cloudflare Worker не было).
Проверка `$HTTPS_PROXY/__agentproxy/status` подтверждает, что
организационный proxy активен (`enabled: true`), но домен
`neuru-telegram-proxy.levshina900.workers.dev` не входит в разрешённый
список (`noProxy`) — блокировка происходит на уровне организационной
egress-политики окружения, а не из-за временного сбоя сети. Это
идентично трём неудачным попыткам за 12.08.2026 (15:43, 18:13, 19:03 UTC) —
проблема не устранена за прошедшие сутки, несмотря на то что была
зафиксирована в логе.

**Состояние:** `state-dzen.json` не создан/не обновлён — пост `1`
по-прежнему считается неопубликованным.

**Требуется:** разрешить исходящий доступ к
`neuru-telegram-proxy.levshina900.workers.dev` (и/или `api.telegram.org`)
в сетевой политике окружения, где выполняется ИИ-Постинг. Без этого
публикация в Telegram невозможна ни при каком количестве повторных
запусков — это уже четвёртая подряд неудачная попытка (3 за 12.08.2026 +
эта), проблема требует ручного вмешательства в конфигурацию egress.

## Попытка публикации — 2026-08-13 (следующий запуск)

- Пост: `post-1.md` (по-прежнему единственный кандидат — файла состояния
  нет, ничего не опубликовано)
- Способ отправки: Cloudflare Worker прокси (как предписано v2.0)
- Результат: **ОШИБКА** — `curl` вернул `CONNECT tunnel failed, response 403`,
  HTTP-статус `000`, соединение не установлено
- Проверка `$HTTPS_PROXY/__agentproxy/status`: прокси активен
  (`enabled: true`), но `neuru-telegram-proxy.levshina900.workers.dev`
  по-прежнему отсутствует в `noProxy` — egress-политика окружения не
  изменилась со вчерашнего дня
- **Состояние:** `state-dzen.json` снова не создан — пост `1` остаётся
  неопубликованным
- Это уже пятая подряд неудачная попытка (3 за 12.08.2026 + 2 за
  13.08.2026). Проблема не устраняется автоматическими повторами —
  требуется ручное изменение сетевой политики (allowlist для домена
  Worker'а или `api.telegram.org`).
