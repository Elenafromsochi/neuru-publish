# Лог публикаций — dzen — 13.08.2026

## Попытка публикации — 2026-08-13 09:05 UTC (12:05 MSK)

- Пост: `post-1.md` (следующий неопубликованный, файла состояния нет)
- Канал: `@HotelAI_ru`
- Способ отправки: Cloudflare Worker прокси
  `https://neuru-telegram-proxy.levshina900.workers.dev` (согласно инструкции v2.0)
- Результат: **ОШИБКА** — публикация не выполнена

**Причина:** та же блокировка egress, что и все три попытки 12.08.2026
(см. `social/posting/12.08.2026/log-dzen.md`). Диагностический `curl` к
`https://neuru-telegram-proxy.levshina900.workers.dev/` и напрямую к
`https://api.telegram.org/` из этой среды (запущено перед попыткой
отправки, реальное сообщение в Telegram не отправлялось) вернул одинаковый
результат:

```
curl: (56) CONNECT tunnel failed, response 403
```

Это подтверждает, что блокировка — на уровне организационной
egress-политики окружения, где выполняется ИИ-Постинг, а не временный
сбой сети. Согласно правилам работы с proxy-агентом (403/407 — не
ретраить, сообщать), повторные попытки в рамках этого запуска не
предпринимались.

**Состояние:** `state-dzen.json` не создан — пост `1` считается
неопубликованным. Это уже 4-я подряд неудачная попытка публикации
(15:43, 18:13, 19:03 UTC 12.08.2026 и 09:05 UTC 13.08.2026).

**Дополнительно обнаружено:** в репозитории есть параллельный механизм
публикации — GitHub Actions workflow `.github/workflows/telegram-posting.yml`
(`Telegram Posting`), запускается по cron `0 9-19 * * *` (то же окно,
12:00–22:00 MSK) и читает/пишет те же файлы
(`social/posting/<дата>/dzen/post-N.md`, `state-dzen.json`). Он публикует
напрямую в `api.telegram.org` через секрет `TELEGRAM_BOT_TOKEN`, минуя
egress-ограничение этой среды. Его последние прогоны:
- 5 прогонов 12.08.2026 — все `failure` (ошибка `HTTP Error 403: Forbidden`
  при старой версии скрипта через Cloudflare Worker прокси).
- 1 прогон 13.08.2026 08:54 UTC — `success`, но это был no-op: на момент
  checkout (`0cb4dc6`) папка `social/posting/13.08.2026/dzen/` ещё не
  существовала (пост одобрили позже, коммит `cedf718`), поэтому скрипт
  ничего не публиковал и не должен был.

Если оба механизма (эта рутина ИИ-Постинга и GitHub Action) продолжат
работать по одному и тому же расписанию и одним и тем же файлам, есть
риск задвоения публикации или конфликтующих коммитов `state-dzen.json`,
как только egress-блокировка в одном из механизмов исчезнет. Стоит
оставить только один рабочий канал публикации.

**Требуется:** решение человека — либо (а) разрешить исходящий доступ к
`api.telegram.org` и/или `neuru-telegram-proxy.levshina900.workers.dev` в
egress-политике среды, где выполняется эта рутина, либо (б) отключить эту
рутину и полагаться только на GitHub Action (после проверки, что
`TELEGRAM_BOT_TOKEN` в секретах репозитория корректен и воркфлоу
действительно публикует пост, а не только делает no-op).
