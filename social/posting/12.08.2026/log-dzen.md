# Лог публикаций — dzen — 12.08.2026

## Попытка публикации — 2026-08-12 15:43 UTC (18:43 MSK)

- Пост: `post-1.md` (следующий неопубликованный, файла состояния не было)
- Канал: `@HotelAI_ru`
- Результат: **ОШИБКА** — публикация не выполнена

**Причина:** сетевой запрос к `https://api.telegram.org` заблокирован
egress-политикой окружения (proxy вернул `403` на CONNECT к
`api.telegram.org:443` — «policy denial»). Запрос не дошёл до Telegram Bot
API, ответа от Telegram не было.

**Состояние:** `state-dzen.json` не создан/не обновлён — пост `1` считается
неопубликованным. При следующем запуске нужно повторить попытку публикации
`post-1.md`.

**Требуется:** разрешить исходящий доступ к `api.telegram.org` в
сетевой политике окружения, где выполняется ИИ-Постинг, либо публиковать
через среду с доступом к Telegram Bot API.

## Попытка публикации — 2026-08-12 18:13 UTC (21:13 MSK)

- Пост: `post-1.md` (следующий неопубликованный, файла состояния всё ещё нет)
- Канал: `@HotelAI_ru`
- Способ отправки: Cloudflare Worker прокси
  `https://neuru-telegram-proxy.levshina900.workers.dev` (согласно
  инструкции v2.0, прямой вызов `api.telegram.org` не пробовали)
- Результат: **ОШИБКА** — публикация не выполнена

**Причина:** сетевой запрос к прокси заблокирован egress-политикой
окружения — proxy вернул `403` на CONNECT к
`neuru-telegram-proxy.levshina900.workers.dev:443` («policy denial or
upstream failure»). Запрос не дошёл ни до Cloudflare Worker, ни тем более
до Telegram Bot API. Диагностика (`curl $HTTPS_PROXY/__agentproxy/status`)
подтверждает блокировку на уровне организационной политики, а не
временный сбой сети — повторные попытки в рамках этого запуска не
предпринимались согласно правилам работы с proxy-агентом (403/407 —
не ретраить, сообщать).

**Состояние:** `state-dzen.json` не создан/не обновлён — пост `1`
по-прежнему считается неопубликованным.

**Требуется:** разрешить исходящий доступ к
`neuru-telegram-proxy.levshina900.workers.dev` в сетевой политике
окружения, где выполняется ИИ-Постинг (домен `api.telegram.org` уже был
заблокирован ранее — см. попытку выше, прокси не решил проблему), либо
запускать ИИ-Постинг в среде с доступом к одному из этих хостов.
